<nav>
  <ul class="pager">
    <li class="previous{{#unless prev}} disabled{{/unless}}">
      <a {{#if prev}}href="{{page_url prev}}"{{/if}}>
        <i class="fa fa-long-arrow-left"></i> Neuere Posts
      </a>
    </li>
    <li class="next{{#unless next}} disabled{{/unless}}">
      <a {{#if next}}href="{{page_url next}}"{{/if}}>
        Ältere Posts <i class="fa fa-long-arrow-right"></i>
      </a>
    </li>
  </ul>
</nav>
